// Test 1: Simple function call (works)
print("test1");

// Test 2: Function call with integer (should work)  
print(42);

// Test 3: Function call with string (should work)
print("hello");

// Test 4: Enum declaration (should work)
enum Color {
    RED
}

// Test 5: Function call with enum (fails)
print(Color.RED);
