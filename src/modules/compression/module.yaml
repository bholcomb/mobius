name: compression
version: 0.1.0
entry:
  script: compression.mob
platforms:
  linux-x86_64:
    module_library: native/linux-x86_64/compression.so
dependencies:
  mobius_modules: []
metadata:
  description: Archive and compression helpers
