name: json
version: 0.1.0
platforms:
  linux-x86_64:
    module_library: native/linux-x86_64/json.so
dependencies:
  mobius_modules: []
metadata:
  description: JSON parsing and serialization helpers
