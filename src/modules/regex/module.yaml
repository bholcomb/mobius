name: regex
version: 0.1.0
platforms:
  linux-x86_64:
    module_library: native/linux-x86_64/regex.so
dependencies:
  mobius_modules: []
metadata:
  description: Regular expression matching and replacement helpers
