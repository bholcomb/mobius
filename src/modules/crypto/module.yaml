name: crypto
version: 0.1.0
platforms:
  linux-x86_64:
    module_library: native/linux-x86_64/crypto.so
dependencies:
  mobius_modules: []
metadata:
  description: Hashing, encoding, UUID, and random helpers
