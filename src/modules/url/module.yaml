name: url
version: 0.1.0
platforms:
  linux-x86_64:
    module_library: native/linux-x86_64/url.so
dependencies:
  mobius_modules: []
metadata:
  description: URL parsing, encoding, and query helpers
