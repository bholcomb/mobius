enum Color {
    RED,
    GREEN,
    BLUE
}

enum Status : uint8 {
    SUCCESS = 0,
    ERROR = 1,
    PENDING = 2
}

var myColor = Color.RED;
var status = Status.SUCCESS;
print(myColor);
print(status);
