This folder holds built `.mz` package archives.

These archives are package artifacts produced by `tools/pack_module.mob`.
They are not automatically loadable by the runtime until they are installed
into a module search root such as `./modules/`.

Current package candidates:

- `sqlite`
- `glfw`
- `imgui`
- `vulkease`
- `monstro`
