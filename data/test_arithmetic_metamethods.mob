// Test arithmetic metamethods
print("=== Testing Arithmetic Metamethods ===");

// Create tables without metamethods first
var table1 = {value: 10};
var table2 = {value: 5};

print("Table1:", table1);
print("Table2:", table2);

// Test that arithmetic fails without metamethods
print("Testing arithmetic without metamethods...");

// This should fail
print("table1 + table2 should fail:");
var result = table1 + table2;
print("Result:", result);

print("=== Arithmetic metamethod tests completed! ===");
