// Test type annotations with uint64
print("=== Type Annotation Tests ===");

// uint64 storing -1 is UINT64_MAX (C semantics)
var x: uint64 = -1;
print(x);

// int64 annotation
var y: int64 = 42;
print(y);

print("Type annotation tests passed");
