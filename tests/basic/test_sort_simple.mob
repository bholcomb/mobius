// Test basic sort (no comparator)
var nums = [5, 3, 1, 4, 2];
nums:sort();
print("sorted:", nums[0], nums[1], nums[2], nums[3], nums[4]);

// Test sort with custom comparator
nums:sort(func(a, b) { return a > b; });
print("desc:", nums[0], nums[1], nums[2], nums[3], nums[4]);

print("done");
