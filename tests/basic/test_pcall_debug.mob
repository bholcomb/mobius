// Test 1: filter alone
var evens = array_filter([1, 2, 3, 4, 5, 6], func(x, i) { return x % 2 == 0; });
print("filter ok, len:", array_length(evens));

// Test 2: simple array literal after filter
print("making array...");
var nums = [10, 20, 30];
print("array ok:", nums[0], nums[1], nums[2]);
print("done");
