// Test #pragma override_behavior "error" (default)
print("=== Testing Override Behavior: ERROR (default) ===");

// Import math module into a namespace
import "math" as m1

print("First import successful");

// Try to import again to the same namespace - should error
print("Trying to re-import to same namespace...");
import "text_processing" as m1

print("This line should not be reached!");

