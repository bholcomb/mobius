// Test fiber.sleep
print("=== Fiber Sleep Tests ===")

import "fiber"

// fiber.sleep(0) should be a no-op
fiber.sleep(0)

// fiber.sleep(1) should complete without error
fiber.sleep(1)

// Negative values should be treated as no-op
fiber.sleep(-1)

print("=== All fiber.sleep tests passed! ===")
